---
- name: Add Workers to the Swarm
  shell: "docker swarm join --token {{ hostvars['localhost']['worker_token']['stdout'] }} {{ hostvars['localhost']['ansible_default_ipv4']['address'] }}:2377"
  tags: swarm